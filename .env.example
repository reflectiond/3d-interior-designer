# Layout editor (F11.x) — secret URL token
#
# The editor opens at `/?editor=1&token=<TOKEN>` only when this value matches.
# In production, override via host environment (Vercel/Netlify/CI secrets) — the
# build picks the host env var over a committed .env file.
VITE_LAYOUT_EDITOR_TOKEN=replace-with-secret
