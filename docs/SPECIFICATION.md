# 3D Interior Designer — Спецификация

**Версия:** 0.1.0-draft
**Дата:** 2026-04-26
**Репозиторий:** https://github.com/reflectiond/3d-interior-designer
**Тип проекта:** ВКР (Выпускная Квалификационная Работа) + коммерческое приложение

---

## 1. Описание продукта

### 1.1 Назначение

Веб-приложение для планирования отделки и расстановки мебели в типовом доме под ключ. Встраивается на сайт строительной компании. Конечный пользователь — заказчик, получивший дом в черновой отделке. Приложение помогает ему составить план действий и предварительную смету по доведению дома до состояния «готов к заселению».

### 1.2 Целевая аудитория

- **Конечный пользователь:** клиент строительной компании, не дизайнер, без опыта работы с CAD.
- **Покупатель приложения:** строительная компания, продающая дома под ключ. Встраивает виджет на свой сайт (Tilda / WordPress / кастомный HTML).

### 1.3 Ключевые требования

- Полное соответствие 2D-схемы и 3D-сцены: цвета, габариты, расположение объектов.
- Возможность последовательно проходить 4 этапа и возвращаться к предыдущим.
- Универсальное встраивание на любой сайт через `<iframe>`.
- Локальное сохранение проектов и экспорт сметы в PDF/PNG.
- Защита от типовых веб-эксплоитов (XSS, CSRF, clickjacking).
- Покрытие тестами всех 4 этапов работы пользователя.

---

## 2. Технологический стек

### 2.1 Frontend

| Слой         | Технология               | Назначение                                              |
| ------------ | ------------------------ | ------------------------------------------------------- |
| Сборка       | Vite                     | Быстрый dev-server, оптимизированный prod-build         |
| Язык         | TypeScript (strict)      | Типобезопасность, самодокументация                      |
| UI           | React 18                 | Компонентная архитектура                                |
| 2D-канвас    | react-konva              | Тайловая схема, drag-and-drop, масштабирование          |
| 3D-сцена     | React Three Fiber + drei | Декларативный 3D на базе Three.js                       |
| State        | Zustand                  | Единый источник правды для проекта                      |
| Стили        | CSS Modules + theme.ts   | Изоляция стилей, единая палитра                         |
| PDF          | jsPDF                    | Экспорт сметы в PDF                                     |
| Скриншоты    | html2canvas              | Скриншот 2D-схемы; для 3D — `gl.domElement.toDataURL()` |
| Sanitization | DOMPurify                | Экранирование пользовательского ввода                   |

### 2.2 Backend (Supabase, минимальный)

| Слой           | Назначение                                                  |
| -------------- | ----------------------------------------------------------- |
| Postgres       | Таблицы: `clients`, `pricing_overrides`, `analytics_events` |
| RLS-политики   | Изоляция данных по `clientId`                               |
| Storage        | Опционально: PDF-архив проектов                             |
| Edge Functions | Опционально: подписанные URL для скачивания PDF             |

### 2.3 Качество кода

| Инструмент                 | Роль                                     |
| -------------------------- | ---------------------------------------- |
| ESLint + typescript-eslint | Статический анализ                       |
| Prettier                   | Форматирование                           |
| Husky + lint-staged        | Pre-commit hooks                         |
| Vitest                     | Unit-тесты                               |
| Playwright                 | E2E + security-тесты                     |
| GitHub Actions             | CI: lint → test → build → deploy preview |

### 2.4 Языки

- **UI:** русский
- **Код, имена переменных, коммиты, комментарии:** английский
- **README, SPECIFICATION, документация в репо:** русский

---

## 3. Архитектура

### 3.1 Модель встраивания

Приложение хостится на одном домене (`designer.your-domain.com`) и встраивается на сайт клиента одной строкой:

```html
<iframe
  src="https://designer.your-domain.com/?clientId=CLIENT_ID"
  width="100%"
  height="800"
  style="border: none;"
  allowfullscreen
></iframe>
```

URL-параметры:

- `clientId` (обязательный) — идентификатор покупателя приложения, определяет прайс и брендинг.
- `projectId` (опционально) — для загрузки сохранённого проекта.
- `lang` (опционально, default `ru`) — язык интерфейса.

**Безопасность iframe:**

- `Content-Security-Policy: frame-ancestors *` (по умолчанию для коммерческой модели) или whitelist по доменам клиентов.
- `X-Frame-Options` НЕ устанавливается (несовместим с frame-ancestors).
- Никаких чувствительных данных в URL — только публичные идентификаторы.

### 3.2 Единая модель данных проекта

Главный принцип архитектуры: **один Zustand-стор хранит весь проект, 2D и 3D — это два рендеринга одних и тех же данных**.

```typescript
type Project = {
  version: '1.0';
  clientId: string;
  projectId: string; // UUID, генерится при создании
  layoutId: 1 | 2 | 3; // одна из трёх типовых планировок
  createdAt: string;
  updatedAt: string;
  currentStage: 1 | 2 | 3 | 4; // текущий этап

  // Заполняется из layout JSON при выборе планировки
  rooms: Room[];
  walls: Wall[];
  electricalPanel: { x: number; y: number }; // позиция щитка в тайлах

  // Этап 2: черновая отделка
  flooring: Record<RoomId, 'screed' | 'screed_heated'>; // по умолчанию screed
  ceiling: Record<RoomId, 'stretch' | 'drywall'>; // по умолчанию stretch
  electricalPoints: ElectricalPoint[]; // выбранные пользователем
  electricalRoutes: ElectricalRoute[]; // вычисленные пути

  // Этап 3: чистовая отделка
  floorCovering: Record<RoomId, 'linoleum' | 'laminate' | 'tile' | 'quartz_vinyl'>;
  wallCovering: Record<WallSegmentId, 'wallpaper' | 'paint'>; // по сегменту стены, не по комнате
  furniture: FurnitureInstance[];
};

type Room = {
  id: RoomId;
  type: 'bathroom' | 'kitchen' | 'boiler' | 'living' | 'bedroom';
  name: string; // RU-название для UI
  tiles: TileCoord[]; // тайлы, входящие в комнату
  area: number; // м², считается из tiles
};

type Wall = {
  id: WallSegmentId;
  type: 'external' | 'internal';
  start: TileCoord;
  end: TileCoord;
  adjacentRooms: RoomId[]; // для external — один, для internal — два
};

type ElectricalPoint = {
  id: string;
  wallId: WallSegmentId;
  position: number; // 0..1 — позиция вдоль стены
  type: 'socket' | 'switch';
};

type ElectricalRoute = {
  pointId: string;
  path: TileCoord[]; // тайлы потолка от щитка/ответвления до точки
};

type FurnitureInstance = {
  id: string; // UUID
  catalogId: string; // ссылка на furniture-catalog.json
  position: TileCoord; // координата левого нижнего угла
  rotation: 0 | 90 | 180 | 270; // градусы
  mirrored: boolean;
};

type TileCoord = { x: number; y: number }; // целые числа, тайлы
```

### 3.3 Тайловая система

- **Размер тайла:** 0.25 м × 0.25 м
- **Начало координат:** левый нижний угол схемы, ось Y направлена вверх (как в математике, не как в экране)
- **Все размеры округлены до тайла:** мебель, стены, точки на стенах
- **Преобразование в 3D:** `world_x = tile_x * 0.25`, `world_z = tile_y * 0.25` (Y в Three.js — вверх)

### 3.4 Цветовое соответствие 2D ↔ 3D

**Единый источник правды — `src/theme/palette.ts`.** Любой цвет, используемый и в 2D, и в 3D, импортируется только отсюда. Хардкод цветов в компонентах запрещён ESLint-правилом.

```typescript
// src/theme/palette.ts
export const PALETTE = {
  // Покрытия пола
  floor: {
    linoleum: '#A89578',
    laminate: '#C9A678',
    tile: '#E8E8E8',
    quartz_vinyl: '#8B7355',
    screed: '#9E9E9E', // на этапе 2, до выбора покрытия
  },
  // Покрытия стен
  walls: {
    wallpaper: '#D9C7A7',
    paint: '#FAFAFA',
    plaster: '#C4C4C4', // черновая
    external: '#5A5A5A', // внешние стены
  },
  // Потолок
  ceiling: {
    stretch: '#F5F5F5',
    drywall: '#E0E0E0',
  },
  // Мебель (категориальные цвета — для условного рендеринга в 2D и 3D)
  furniture: {
    sofa: '#6B7C8C',
    bed: '#8B7355',
    table: '#A0826D',
    chair: '#3D3D3D',
    wardrobe: '#7A6240',
    chair_office: '#2C2C2C',
    desk: '#A0826D',
    kitchen_lower: '#D4C4A8',
    kitchen_upper: '#D4C4A8',
    kitchen_column: '#D4C4A8',
    toilet: '#FFFFFF',
    sink: '#FFFFFF',
  },
  // Электрика
  electrical: {
    wire: '#FFB800',
    socket: '#FF6B35',
    switch: '#FF9F40',
    panel: '#D32F2F',
  },
  // Зоны комнат (фоновая заливка в 2D)
  rooms: {
    bathroom: '#E1F5FE',
    kitchen: '#FFF3E0',
    boiler: '#FFEBEE',
    living: '#F3E5F5',
    bedroom: '#E8F5E9',
  },
} as const;
```

**Контракт:** для каждого ключа в `PALETTE` существует ровно один цвет, который применяется в `react-konva` (атрибут `fill`) и в `react-three-fiber` (`<meshStandardMaterial color={...}/>`). E2E-тест проверяет, что для одного и того же объекта цвет на скриншоте 2D-вида совпадает с цветом в 3D-виде (с учётом освещения — допускается дельта в HSV).

### 3.5 Структура репозитория

```
3d-interior-designer/
├── .github/
│   └── workflows/
│       ├── ci.yml              # lint + test + build на каждый PR
│       └── deploy.yml          # deploy на main
├── docs/
│   ├── SPECIFICATION.md        # этот файл
│   ├── ARCHITECTURE.md         # детальные диаграммы
│   ├── DECISIONS.md            # ADR — Architecture Decision Records
│   └── PROGRESS.md             # статус по требованиям, обновляется агентом
├── public/
│   └── embed-example.html      # демо-страница для тестирования iframe
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── store/
│   │   └── projectStore.ts     # Zustand store
│   ├── theme/
│   │   ├── palette.ts          # единая палитра 2D=3D
│   │   └── tokens.ts           # размеры, отступы
│   ├── data/
│   │   ├── layouts/
│   │   │   ├── layout_1.json   # типовая планировка №1 (заполняется Таей)
│   │   │   ├── layout_2.json
│   │   │   └── layout_3.json
│   │   ├── furniture-catalog.json   # каталог мебели (агент + Тая)
│   │   └── pricing.default.json     # дефолтный прайс
│   ├── stages/
│   │   ├── Stage1LayoutSelection/
│   │   ├── Stage2RoughFinish/
│   │   ├── Stage3FineFinish/
│   │   └── Stage4Summary/
│   ├── views/
│   │   ├── View2D/             # react-konva
│   │   └── View3D/             # react-three-fiber
│   ├── domain/                 # чистая логика, без UI
│   │   ├── electrical/
│   │   │   └── pathfinder.ts   # BFS по тайлам потолка
│   │   ├── pricing/
│   │   │   └── estimator.ts    # расчёт сметы
│   │   ├── furniture/
│   │   │   └── placement.ts    # валидация размещения
│   │   └── geometry/
│   │       └── tiles.ts        # утилиты тайлов
│   ├── export/
│   │   ├── pdfExporter.ts
│   │   └── imageExporter.ts
│   ├── persistence/
│   │   ├── localStorage.ts
│   │   ├── jsonExportImport.ts
│   │   └── supabaseClient.ts   # только для прайсов
│   └── i18n/
│       └── ru.ts
├── tests/
│   ├── unit/                   # Vitest
│   │   ├── pathfinder.test.ts
│   │   ├── estimator.test.ts
│   │   └── placement.test.ts
│   └── e2e/                    # Playwright
│       ├── stage1.spec.ts
│       ├── stage2.spec.ts
│       ├── stage3.spec.ts
│       ├── stage4.spec.ts
│       ├── 2d-3d-color-parity.spec.ts
│       └── security.spec.ts    # XSS, clickjacking, CSP
├── .eslintrc.cjs
├── .prettierrc
├── tsconfig.json
├── vite.config.ts
├── package.json
└── README.md
```

---

## 4. Функциональные требования

### 4.1 Этап 1 — Выбор планировки

| ID   | Требование                                                                                                                       |
| ---- | -------------------------------------------------------------------------------------------------------------------------------- |
| F1.1 | Пользователю показываются 3 типовые планировки в виде превью (миниатюры 2D-схем).                                                |
| F1.2 | Каждая планировка имеет название, общую площадь, перечень комнат.                                                                |
| F1.3 | Выбор планировки загружает её данные (rooms, walls, electricalPanel) в стор.                                                     |
| F1.4 | После выбора пользователь видит выбранную планировку на полном экране 2D-вида.                                                   |
| F1.5 | На схеме отображены: внешние стены, межкомнатные стены, заливка зон комнат цветами из `PALETTE.rooms`, иконка электрощитка.      |
| F1.6 | Кнопка «Далее» переводит на этап 2.                                                                                              |
| F1.7 | Смена планировки на этапе 1 после уже сделанной работы на 2/3/4 показывает confirm-диалог («Все ваши изменения будут сброшены»). |

### 4.2 Этап 2 — Черновая отделка

#### 4.2.1 Стяжка пола

| ID     | Требование                                                                                      |
| ------ | ----------------------------------------------------------------------------------------------- |
| F2.1.1 | Для каждой комнаты пользователь выбирает: `screed` (по умолчанию) или `screed_heated`.          |
| F2.1.2 | UI: список комнат сбоку, у каждой — два радио-чекбокса.                                         |
| F2.1.3 | На 2D-схеме комнаты с тёплым полом подсвечиваются (например, рамкой `PALETTE.electrical.wire`). |

#### 4.2.2 Электрика

| ID     | Требование                                                                                                                                                                                                                                                  |
| ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F2.2.1 | Пользователь видит позицию электрощитка (иконка).                                                                                                                                                                                                           |
| F2.2.2 | Клик по тайлу, прилегающему к стене (с внутренней стороны), создаёт электроточку.                                                                                                                                                                           |
| F2.2.3 | Точка может быть `socket` или `switch` — переключатель в UI.                                                                                                                                                                                                |
| F2.2.4 | После создания точки автоматически вычисляется маршрут провода: BFS по тайлам потолка от щитка ИЛИ от ближайшей точки на уже существующем дереве проводки до новой точки. Маршрут — кратчайший по Манхэттенской метрике, движение только по 4 направлениям. |
| F2.2.5 | Маршрут отрисовывается на 2D-схеме линиями цвета `PALETTE.electrical.wire`.                                                                                                                                                                                 |
| F2.2.6 | В 3D-сцене провод — цилиндр диаметром 0.02 м того же цвета, идёт по потолку (Y = высота комнаты, по умолчанию 2.7 м).                                                                                                                                       |
| F2.2.7 | Удаление точки: пересчитывается всё дерево маршрутов.                                                                                                                                                                                                       |
| F2.2.8 | Нельзя ставить точку на внешней стене дома, выходящей наружу (в спецификации каждой планировки помечается, какие стены допустимы).                                                                                                                          |

#### 4.2.3 Штукатурка

| ID     | Требование                                                                               |
| ------ | ---------------------------------------------------------------------------------------- |
| F2.3.1 | Применяется автоматически ко всем стенам, выбора у пользователя нет.                     |
| F2.3.2 | На 2D отображается информационная плашка: «Штукатурка стен — выполняется автоматически». |
| F2.3.3 | Учитывается в смете (площадь всех стен × цена за м²).                                    |

#### 4.2.4 Потолок

| ID     | Требование                                                                                                |
| ------ | --------------------------------------------------------------------------------------------------------- |
| F2.4.1 | Для каждой комнаты пользователь выбирает: `stretch` (натяжной, по умолчанию) или `drywall` (гипсокартон). |
| F2.4.2 | UI аналогичен стяжке.                                                                                     |
| F2.4.3 | В 3D-сцене потолок отображается соответствующим цветом из `PALETTE.ceiling`.                              |

#### 4.2.5 Просмотр в 3D

| ID     | Требование                                                                                                                                 |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------ |
| F2.5.1 | Кнопка «Посмотреть в 3D» в любой момент этапа 2.                                                                                           |
| F2.5.2 | 3D-сцена показывает: пол с покрытием (на этапе 2 — серый screed), стены (плашка «штукатурка»), потолок выбранного типа, провода электрики. |
| F2.5.3 | OrbitControls для вращения камеры.                                                                                                         |
| F2.5.4 | Базовое освещение: ambient + 1 directional.                                                                                                |

### 4.3 Этап 3 — Чистовая отделка

#### 4.3.1 Покрытие пола

| ID     | Требование                                                                   |
| ------ | ---------------------------------------------------------------------------- |
| F3.1.1 | Для каждой комнаты выбор из: `linoleum`, `laminate`, `tile`, `quartz_vinyl`. |
| F3.1.2 | Цвет в 2D и 3D из `PALETTE.floor`.                                           |

#### 4.3.2 Покрытие стен

| ID     | Требование                                                                                                                                                    |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F3.2.1 | Стена — это сегмент. Каждый сегмент стены может иметь до двух покрытий (по одному с каждой стороны для внутренних стен).                                      |
| F3.2.2 | Выбор: `wallpaper`, `paint`.                                                                                                                                  |
| F3.2.3 | UI: режим «выбор стены» — пользователь кликает по сегменту в 2D, открывается панель с двумя селекторами (по одному на каждую сторону, если стена внутренняя). |
| F3.2.4 | В 3D каждая сторона стены рендерится отдельным `<mesh>` с соответствующим материалом.                                                                         |

#### 4.3.3 Расстановка мебели

| ID     | Требование                                                                                                                                        |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| F3.3.1 | Каталог мебели: палитра в боковой панели с превью каждого предмета.                                                                               |
| F3.3.2 | Drag-and-drop: захват из каталога, перетаскивание на 2D-схему.                                                                                    |
| F3.3.3 | При перетаскивании — подсветка валидной/невалидной зоны (тайлы под мебелью зелёные/красные).                                                      |
| F3.3.4 | Размещение запрещено: пересечение со стеной, выход за пределы комнаты, нарушение правила привязки к комнате (унитаз — только в bathroom).         |
| F3.3.5 | После размещения мебель можно: переместить (drag), повернуть на 90° (горячая клавиша R или кнопка), отзеркалить (M или кнопка), удалить (Delete). |
| F3.3.6 | В 3D мебель отображается как примитивные геометрии (box + цвет из `PALETTE.furniture`). На v1 не требуется реалистичная 3D-модель.                |
| F3.3.7 | Нет ограничения на количество предметов одного типа.                                                                                              |

#### 4.3.4 Каталог мебели

См. `furniture-catalog.json`. Каждый предмет:

```json
{
  "id": "sofa",
  "name": "Диван",
  "category": "living_room",
  "size_tiles": { "w": 8, "h": 4 }, // 2.0 × 1.0 м
  "height_m": 0.85,
  "rotatable": true,
  "mirrorable": true,
  "color_key": "sofa", // ссылка на PALETTE.furniture
  "allowed_rooms": ["living", "bedroom"], // null = любая
  "price_min": 30000,
  "price_max": 120000
}
```

Список (минимальный для v1, размеры округлены до 0.25 м):

- Обеденный стол (1.6 × 0.8 м)
- Стул (0.5 × 0.5 м)
- Диван (2.0 × 1.0 м)
- Кровать (2.0 × 1.6 м)
- Шкаф (1.5 × 0.6 м)
- Компьютерное кресло (0.6 × 0.6 м)
- Рабочий стол (1.4 × 0.7 м)
- Кухонный гарнитур: нижний шкаф (0.6 × 0.6 м, тайлируется), верхний шкаф (0.6 × 0.4 м), колонна (0.6 × 0.6 м)
- Унитаз (0.7 × 0.4 м, allowed_rooms: ["bathroom"])
- Раковина (0.5 × 0.4 м, allowed_rooms: ["bathroom"])

### 4.4 Этап 4 — Итоги

| ID   | Требование                                                                                                                                              |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F4.1 | Список действий (To-Do), сгруппированный по этапам: «Черновая отделка», «Чистовая отделка».                                                             |
| F4.2 | Каждая позиция: количество/площадь + диапазон цены. Например: «Стяжка пола 40 м² — 60 000–90 000 ₽».                                                    |
| F4.3 | Внутри группы порядок отражает технологический порядок работ (стяжка → электрика → штукатурка → потолок; затем покрытие пола → покрытие стен → мебель). |
| F4.4 | Итоговая сумма: диапазон («Итого: 580 000 – 1 250 000 ₽»).                                                                                              |
| F4.5 | Кнопка «Сохранить как PDF» — генерация многостраничного PDF: страница 1 — план 2D, страница 2 — скриншот 3D, страница 3 — смета.                        |
| F4.6 | Кнопка «Сохранить как PNG» — экспорт текущего 2D-вида со сметой.                                                                                        |
| F4.7 | Кнопка «Сохранить проект» — выгрузка JSON-файла с полным состоянием проекта.                                                                            |
| F4.8 | Кнопка «Загрузить проект» (доступна на любом этапе) — импорт JSON. Валидация схемы при импорте через Zod.                                               |

### 4.5 Навигация между этапами

| ID   | Требование                                                                                                                                                       |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F5.1 | Шаги отображаются в шапке. Текущий — выделен.                                                                                                                    |
| F5.2 | Можно вернуться на любой предыдущий этап без потери данных следующих этапов (если не было сброса по F1.7).                                                       |
| F5.3 | Изменение, делающее следующие этапы невалидными (например, удаление комнаты в редакторе — но в v1 это невозможно), сбрасывает зависимые данные с подтверждением. |
| F5.4 | Прогресс автосохраняется в `localStorage` при каждом значимом изменении (debounced 500 мс).                                                                      |

---

## 5. Нефункциональные требования

### 5.1 Производительность

- Стартовая загрузка приложения: < 3 с на 4G.
- 2D-канвас должен поддерживать 60 FPS при ≤ 200 элементах.
- 3D-сцена: 30 FPS на интегрированной графике для типового дома (~60 м²).

### 5.2 Совместимость

- Браузеры: Chrome, Firefox, Safari, Edge — последние 2 мажорные версии.
- Минимальный десктоп. Mobile-поддержка — out of scope для ВКР, но в коде не делать допущений, ломающих мобайл.

### 5.3 Безопасность

| Угроза                                   | Мера                                                                                                                                            |
| ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| XSS через пользовательский ввод          | DOMPurify на всех текстовых полях; строгая типизация; React по умолчанию экранирует                                                             |
| Code injection через импорт JSON-проекта | Zod-валидация схемы; запрет любых полей, не описанных в типе                                                                                    |
| Clickjacking                             | `Content-Security-Policy: frame-ancestors` whitelist; для коммерческой модели — конфигурируется per clientId                                    |
| CSRF                                     | Не релевантно (нет state-changing API на стороне сервера в MVP)                                                                                 |
| Information disclosure через URL         | Никаких токенов в URL, только публичные ID                                                                                                      |
| Supply chain                             | `npm audit` в CI, Dependabot включён                                                                                                            |
| CSP                                      | `default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; connect-src 'self' https://*.supabase.co` |
| LocalStorage poisoning                   | Валидация при чтении, fallback на чистый стейт при ошибке парсинга                                                                              |

### 5.4 Доступность (a11y) — minimal viable

- Все интерактивные элементы доступны с клавиатуры.
- ARIA-метки на иконочных кнопках.
- Контраст текста ≥ 4.5:1.

---

## 6. Тестирование

### 6.1 Unit-тесты (Vitest) — обязательное покрытие

- `domain/electrical/pathfinder.ts` — алгоритм построения маршрутов; ≥ 90% line coverage.
- `domain/pricing/estimator.ts` — расчёт сметы; все типы статей расхода.
- `domain/furniture/placement.ts` — все правила валидации (стены, границы, attribute attachment).
- `domain/geometry/tiles.ts` — преобразования координат.
- `persistence/jsonExportImport.ts` — round-trip: сохранение → загрузка должны давать идентичный объект.

### 6.2 E2E-тесты (Playwright) — сценарии

| ID    | Сценарий                                                                                                                                                              |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E2E-1 | Полный сквозной сценарий: выбрать планировку 1 → пройти все 4 этапа → сохранить PDF → проверить, что PDF не пустой.                                                   |
| E2E-2 | Возврат на этап 2 с этапа 4 → изменение типа потолка → пересчёт сметы.                                                                                                |
| E2E-3 | Электрика: создать 5 точек → проверить, что все имеют маршруты, нет дублирования проводов.                                                                            |
| E2E-4 | Расстановка мебели: попытка поставить унитаз в кухню → отказ.                                                                                                         |
| E2E-5 | Перетаскивание дивана за пределы комнаты → отказ с подсветкой.                                                                                                        |
| E2E-6 | Поворот мебели на 90° четыре раза возвращает в исходное положение.                                                                                                    |
| E2E-7 | Цветовое соответствие 2D ↔ 3D: расставить мебель → скриншот 2D → переключиться в 3D с фиксированной камерой → сравнение цветов в ключевых точках (с допуском по HSV). |
| E2E-8 | Экспорт-импорт JSON: сохранить → загрузить → состояние идентично.                                                                                                     |
| E2E-9 | Перезагрузка страницы: проект восстановлен из localStorage.                                                                                                           |

### 6.3 Security-тесты (Playwright)

| ID    | Сценарий                                                                                                                                                |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SEC-1 | XSS: попытка импортировать JSON с полем `<script>alert(1)</script>` → не должно выполниться.                                                            |
| SEC-2 | XSS: попытка ввести в поле имени проекта `<img src=x onerror=alert(1)>` → отрисовка как текст.                                                          |
| SEC-3 | Clickjacking: запрос с `X-Frame-Options: SAMEORIGIN` от чужого домена должен быть отклонён CSP frame-ancestors (тестируется на стейджинге с whitelist). |
| SEC-4 | localStorage tampering: подменить ключ проекта на невалидный JSON → приложение запускается с дефолтным состоянием, не падает.                           |
| SEC-5 | URL injection: `?clientId=<script>...</script>` → параметр санитизирован.                                                                               |
| SEC-6 | Schema injection: импорт JSON с неизвестными полями → отклонён валидатором.                                                                             |

### 6.4 CI Pipeline (`.github/workflows/ci.yml`)

1. `npm ci`
2. `npm run lint`
3. `npm run typecheck`
4. `npm run test:unit -- --coverage`
5. `npm run build`
6. `npx playwright install --with-deps`
7. `npm run test:e2e`
8. `npm audit --audit-level=high`

PR не может быть смержен без зелёного CI.

---

## 7. Git Workflow

### 7.1 Ветки

- `main` — защищённая, только через PR с зелёным CI и approve.
- `develop` — текущая интеграционная (опционально, можно работать напрямую с `main` через feature-branches).
- `feature/<scope>-<short-name>` — новая фича. Пример: `feature/stage2-electrical-routing`.
- `fix/<short-name>` — багфикс.
- `chore/<short-name>` — инфраструктура, зависимости.

### 7.2 Commits

- Conventional Commits: `feat: …`, `fix: …`, `chore: …`, `docs: …`, `test: …`, `refactor: …`.
- Английский язык.
- Тело коммита (опционально) — что и зачем; не «как» (это видно из diff).

### 7.3 Pull Requests

- Описание PR содержит: ссылку на ID требования из спецификации, скриншоты UI-изменений, чек-лист тестов.
- Один PR = одна логическая единица.
- Squash merge.

### 7.4 Релизы и теги

- Semver: `vMAJOR.MINOR.PATCH`.
- Тег ставится при завершении каждого этапа (или раньше при значимом milestone):
  - `v0.1.0` — этап 1 готов и задокументирован
  - `v0.2.0` — этап 2 готов
  - `v0.3.0` — этап 3 готов
  - `v0.4.0` — этап 4 + экспорт
  - `v1.0.0` — все требования + тесты + защита диплома
- При каждом теге GitHub Release с changelog'ом, генерируемым из conventional commits.

### 7.5 Обновление спецификации

**После каждого тега агент обязан:**

1. Обновить `docs/PROGRESS.md` — статус каждого требования (✅ Done / 🚧 In Progress / ⏳ Planned / ❌ Blocked) со ссылкой на PR.
2. Обновить `docs/SPECIFICATION.md`, если в ходе реализации требование уточнилось — добавить пометку «Уточнено в vX.Y.Z: …».
3. Если изменилось архитектурное решение — добавить ADR в `docs/DECISIONS.md` (формат: контекст → решение → последствия).

---

## 8. Прайсинг

### 8.1 Дефолтный прайс (`pricing.default.json`)

```json
{
  "version": "1.0",
  "currency": "RUB",
  "source": "Средние розничные цены по г. Челябинск, апрель 2026",
  "rough_finish": {
    "screed_per_m2": { "min": 1500, "max": 2500 },
    "screed_heated_per_m2": { "min": 3500, "max": 5500 },
    "electrical_per_m": { "min": 800, "max": 1500 },
    "plaster_per_m2": { "min": 600, "max": 1200 },
    "ceiling_stretch_per_m2": { "min": 700, "max": 1500 },
    "ceiling_drywall_per_m2": { "min": 1200, "max": 2200 }
  },
  "fine_finish": {
    "floor_linoleum_per_m2": { "min": 600, "max": 1500 },
    "floor_laminate_per_m2": { "min": 1200, "max": 3500 },
    "floor_tile_per_m2": { "min": 1500, "max": 4500 },
    "floor_quartz_vinyl_per_m2": { "min": 2000, "max": 5000 },
    "wall_wallpaper_per_m2": { "min": 400, "max": 1800 },
    "wall_paint_per_m2": { "min": 300, "max": 1200 }
  },
  "furniture": {
    "sofa": { "min": 30000, "max": 120000 },
    "bed": { "min": 25000, "max": 95000 },
    "table": { "min": 8000, "max": 35000 },
    "chair": { "min": 2500, "max": 15000 },
    "wardrobe": { "min": 18000, "max": 80000 },
    "chair_office": { "min": 8000, "max": 45000 },
    "desk": { "min": 7000, "max": 30000 },
    "kitchen_lower": { "min": 15000, "max": 50000 },
    "kitchen_upper": { "min": 8000, "max": 30000 },
    "kitchen_column": { "min": 20000, "max": 70000 },
    "toilet": { "min": 6000, "max": 30000 },
    "sink": { "min": 4000, "max": 25000 }
  }
}
```

**Цены для ВКР — индикативные. В защите фиксируется источник и дата сбора.** Реальные цены в коммерческой версии переопределяются через таблицу `pricing_overrides` в Supabase по `clientId`.

### 8.2 Алгоритм расчёта сметы

1. Для каждой комнаты площадь = `tiles.length × 0.0625`.
2. Стяжка: `area × screed_*_per_m2`.
3. Электрика: длина всех маршрутов в метрах × `electrical_per_m`.
4. Штукатурка: суммарная площадь стен (длина × высота 2.7 м) × `plaster_per_m2`.
5. Потолок: `area × ceiling_*_per_m2`.
6. Покрытие пола: `area × floor_*_per_m2`.
7. Покрытие стен: для каждой стороны — `length × 2.7 × wall_*_per_m2`.
8. Мебель: для каждой расставленной единицы — `furniture[type]`.
9. Итог = сумма min, сумма max.

---

## 9. Дистрибуция (план для будущего, не для MVP)

- **MVP (v1.0):** один публичный домен, бесплатный для всех, для защиты ВКР.
- **Коммерческая модель (v2.0):** SaaS — клиент покупает доступ, получает свой `clientId`, привязанный к whitelist-домену. Через Supabase подгружается кастомный прайс.
- **Возможные модели pricing:** месячная подписка, разовая покупка лицензии, revenue share с генерируемых лидов.

---

## 10. Open Questions / Future Work

1. **Админка для клиента** (загрузка своих планировок, своих цен) — v2.
2. **Реалистичные 3D-модели мебели** (glTF из бесплатных библиотек) — v2.
3. **Интеграция с маркетплейсами** (клик по «Купить диван» → переход на партнёрский магазин) — v2.
4. **Совместное редактирование** (несколько устройств) — v2 через Supabase auth.
5. **Мобильная версия** — отдельная разработка.

---

## 11. Глоссарий

| Термин                   | Значение                                                       |
| ------------------------ | -------------------------------------------------------------- |
| Тайл                     | Единичная клетка сетки 0.25×0.25 м, базовая единица измерения. |
| Планировка (layout)      | Одна из трёх типовых конфигураций дома.                        |
| Этап (stage)             | Один из 4 шагов взаимодействия пользователя.                   |
| Маршрут (route)          | Путь электропровода от щитка/ответвления до точки.             |
| Точка (electrical point) | Розетка или выключатель на стене.                              |
| Сегмент стены            | Прямой участок стены между двумя пересечениями/углами.         |
| Чистовая отделка         | Финальная отделка: пол, стены, мебель.                         |
| Черновая отделка         | Стяжка, электрика, штукатурка, потолок.                        |

---

_Спецификация обновляется при каждом теге релиза. История изменений ведётся через git и `docs/PROGRESS.md`._
